# Postgres password for the authentik database
POSTGRES_PASSWORD=change_me_strong_password

# Secret key for authentik (generate with: openssl rand -base64 36)
AUTHENTIK_SECRET_KEY=change_me_random_secret_key
